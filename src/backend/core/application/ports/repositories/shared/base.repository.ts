import { BaseRepositoryErrors } from "./base-repository.errors";

/**
 * Shared base class for repository adapters.
 *
 * Provides common error helpers shared by repository adapters.
 */
export abstract class BaseRepository extends BaseRepositoryErrors {}
